apiVersion: v1
kind: Service
metadata:
  name: dynamodb-local
  namespace: dynamodb-local
spec:
  selector:
    app: dynamodb-local
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
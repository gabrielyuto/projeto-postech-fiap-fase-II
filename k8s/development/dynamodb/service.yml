apiVersion: v1
kind: Service
metadata:
  name: dynamodb
  namespace: development
spec:
  type: ClusterIP
  selector:
    app: dynamodb
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP

apiVersion: apps/v1
kind: Deployment
metadata:
  name: targeting-service
  namespace: development
  labels:
    app: targeting-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: targeting-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: targeting-service
    spec:
      containers:
      - name: targeting-service
        image: projeto-postech-fiap-fase-ii-targeting-service:latest
        imagePullPolicy: Never
        ports:
        - name: http
          containerPort: 8003
        envFrom:
        - configMapRef:
            name: targeting-service-config
        - secretRef:
            name: targeting-service-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 5
          periodSeconds: 10
      
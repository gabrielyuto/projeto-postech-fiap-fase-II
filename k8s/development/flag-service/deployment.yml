apiVersion: apps/v1
kind: Deployment
metadata:
  name: flag-service
  namespace: development
  labels:
    app: flag-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flag-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: flag-service
    spec:
      containers:
      - name: flag-service
        image: projeto-postech-fiap-fase-ii-flag-service:latest
        imagePullPolicy: Never
        ports:
        - name: http
          containerPort: 8002
        envFrom:
        - configMapRef:
            name: flag-service-config
        - secretRef:
            name: flag-service-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 5
          periodSeconds: 10
      